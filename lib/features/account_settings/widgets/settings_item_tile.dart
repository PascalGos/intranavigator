import 'package:flutter/material.dart';

class SettingsItemTile extends StatelessWidget {
  const SettingsItemTile({super.key});

  @override
  Widget build(BuildContext context) {
    //TODO: Write Settings Item Tile
    return const ListTile();
  }
}
